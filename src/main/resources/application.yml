spring:
  data:
    mongodb:
      database: flatsharing
      uri: mongodb+srv://tamirlansemsk:flatsharing@diploma-cluster.anrsmn9.mongodb.net

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs.json
  swagger-ui:
    path: /swagger
    operations-sorter: alpha
    tags-sorter: alpha

logging:
  level:
    org.springframework.*: warn
    org.hibernate.*: warn
    com.zaxxer.*: warn
    org.mongodb.*: warn

